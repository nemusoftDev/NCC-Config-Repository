server:
 port: 9000

profiles: prod
comment: hello car server, i am production !

logging:
 file:
  name: ${user.dir}/log/test.log # api/log/
  max-history: 7
  max-size: 10MB
 level:
  ncc.nemu.main: error